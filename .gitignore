# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Executable file, build with `go build`
webgo

# Output of the go coverage tool, specifically when used with IDE
*.out
.idea
.vscode
